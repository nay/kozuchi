<% @deal.modify_errors_for_complex_form %>
<%= deal_form '明細(複数)',
              :url => complex_deals_path,
              :html => {data: {result_url: @deal.new_record? ? new_deal_path() : monthly_deals_path(year: '_YEAR_', month: '_MONTH_')}} do |f| %>
  <%= render :partial => 'complex_deal_form_contents', :locals => {:f => f} %>
<% end %>
<%= error_messages_for :deal %>
