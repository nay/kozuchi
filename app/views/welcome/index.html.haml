.clearfix
  #overview
    %h2 きちんと。気楽に。
    %p Web家計簿「小槌」を使えば、本格的な家計簿を、気楽に続けられます。
    %p= link_to 'アカウント登録して使い始める（無料）', signup_path, class: %w(btn btn-primary)

  #entrance
    %h3 アカウントをお持ちの方
    = form_tag login_post_path, roll: 'form' do
      .form-group
        = label_tag :login, "ログインID"
        = text_field_tag :login, flash['login'], class: 'form-control'
      .form-group
        = label_tag :password, "パスワード"
        = password_field_tag 'password', '', class: 'form-control'
      .checkbox
        %label
          = check_box_tag 'remember_me'
          ログイン状態を記憶（２週間）
      .error
        = flash['error']
      = submit_tag "ログイン", |
        :id => 'loginButton', |
        :onclick => "if ($('#login').val()=='') {alert('ログインIDを入力してください。'); return false;}if ($('#password').val()=='') {alert('パスワードを入力してください。'); return false;}", |
        :class => %w(btn btn-default) |
      %ul.nav.nav-pills
        %li= link_to 'パスワードを忘れたとき', forgot_password_path
        - if @privacy_policy_setting.show
          %li= link_to 'プライバシーポリシー', privacy_policy_path
