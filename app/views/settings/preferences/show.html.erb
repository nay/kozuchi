<div class="top_box">
  <%= form_for @preferences, :as => :preferences, :url => settings_preferences_path, :html => {:method => :put} do |f| %>
    <div class="form_box">
      <table class="form_fields horizontal">
        <tr>
          <th>ユーザーカラー（#ff0000, red など）</th>
          <td>
            <%= f.text_field :color, :size => 20 %>
            <p class="guide">左上のユーザー表示色を変更できます。</p>
          </td>
        </tr>
        <tr>
          <th>事業利用</th>
          <td>
            <%= f.check_box :business_use %>事業利用
            <p class="guide">「事業利用」にチェックすると、資本金の概念が導入されます。チェックを外すと、資本金口座は債権口座に自動変更されます。</p>
          </td>
        </tr>
        <tr>
          <th>項目の左右配置のスタイル</th>
          <td>
            <label><%= f.radio_button :bookkeeping_style, 1, :checked => current_user.preferences.bookkeeping_style? %>簿記方式（左に借方、右に貸方を表示）</label>
            <label><%= f.radio_button :bookkeeping_style, 0, :checked => !current_user.preferences.bookkeeping_style? %>小槌方式（出金が左、入金が右）</label>
            <p class="guide">簿記同様の左右配置に統一するには、簿記方式を選択してください。</p>
          </td>
        </tr>
      </table>
      
      <div class="buttons_line"><%= f.submit 'カスタマイズ設定を更新', :class => 'button' %></div>
    </div>
  <% end  %>
</div>
