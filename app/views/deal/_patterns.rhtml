<% if @patterns && !@patterns.empty? %>
  <div style="background-color:white;">
    <table>
    <% for pattern in @patterns %>
      <tr>
        <td><div class="clickable_text" onClick="setEditDeal('<%= pattern.summary %>', '<%= pattern.account_entries[1].amount%>', '<%= pattern.account_entries[0].account_id%>', '<%= pattern.account_entries[1].account_id%>');"><%= pattern.summary %></div></td>
        <td><%= number_with_delimiter(pattern.account_entries[1].amount) %></td>
        <td>[<%= pattern.account_entries[0].account.name %>]</td>
        <td> → </td>
        <td>[<%= pattern.account_entries[1].account.name %>]</td>
      </tr>
    <% end %>
    </table>
  </div>
<% else %>
  <br />
<% end %>